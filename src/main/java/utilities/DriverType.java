package utilities;

/**
 * This enum has the Drivers for the application, all the new drivers should be
 * added here
 */
public enum DriverType {
	JLTABEMULATOR, IOS, CHROME, MAGENTO, FARMERBROWSERSTACK, MCBROWSERSTACK, MCEMULATOR, JIVAWEBPORTAL, FARMERCRM,
	FARMEREMULATOR;
}
